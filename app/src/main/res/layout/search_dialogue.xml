<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:id="@+id/searchDialog">

    <EditText
        android:id="@+id/searchEditText"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:hint="@string/search_text"
        android:inputType="textCapWords"
        android:paddingLeft="@dimen/half_padding"
        android:paddingRight="@dimen/half_padding" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/englishSelector"
            android:layout_width="0dp"
            android:padding="@dimen/half_padding"
            android:layout_margin="@dimen/half_padding"
            android:background="@drawable/rounded"
            android:gravity="center"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/search_english"/>

        <TextView
            android:id="@+id/japaneseSelector"
            android:layout_width="0dp"
            android:padding="@dimen/half_padding"
            android:layout_margin="@dimen/half_padding"
            android:gravity="center"
            android:background="@drawable/rounded"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/search_japanese"/>
    </LinearLayout>
</LinearLayout>